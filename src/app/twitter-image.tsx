// X/Twitter uses the same 1200×630 card as Open Graph, so this route reuses
// the generator wholesale rather than maintaining a second design.
export { default, alt, size, contentType } from "./opengraph-image";
