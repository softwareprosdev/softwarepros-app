// X/Twitter renders the same 1200×630 card, so this reuses the generator
// rather than maintaining a second design that would drift.
export { default, alt, size, contentType } from "./opengraph-image";
